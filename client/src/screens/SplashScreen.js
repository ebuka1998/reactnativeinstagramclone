import React from 'react'
import { View, Text } from 'react-native'

const SplashScreen = () => {
    return (
        <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
            <Text style={{fontSize: 50, color: 'green'}}>Splash Screen</Text>
        </View>
    )
}

export default SplashScreen
